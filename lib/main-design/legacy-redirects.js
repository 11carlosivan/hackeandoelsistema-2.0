export const legacyRedirects = [
  /*
   * WordPress migration map.
   * Add exported URLs here as permanent 301 redirects once the crawler/export
   * gives us the real legacy slugs.
   *
   * Example:
   * {
   *   source: '/2024/10/04/old-wordpress-slug/',
   *   destination: '/articulo/new-canonical-slug',
   *   permanent: true,
   * },
   */
];
